/**
 * PTE theme — extends qulto. No listable-component overrides at this time.
 * All listable components are inherited from qulto via the `extends: qulto` config.
 */
export const LISTABLE_COMPONENTS = [];
